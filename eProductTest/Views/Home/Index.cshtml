@model eStoreProductListViewModel

@foreach(var product in Model.Products ?? Enumerable.Empty<Product>())
{
    @* <div class="card border-dark-subtle m-1 p-1">
        <div class="bg-light p-1">
            <h4>
                @product.Name
                <span class="badge rounded-pill bg-primary text-white" style="float:right">
                    <small>@product.Price.ToString("c")</small>
                </span>
            </h4>
        </div>
        <div class="card-text p-1">@product.Description</div>
    </div> *@
    <partial name="ProductSummary" model="product" />
}

<div page-model="@Model.PagingInfo" page-action="Index"
    page-class-enabled="true" page-class="btn m-1" page-class-normal="btn-outline-dark" page-class-selected="btn-primary"
    page-url-category="@Model.CurrentCategory!"
>
</div>